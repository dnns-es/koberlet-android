{
  "name": "koberlet-android",
  "version": "0.56.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "_comment_canales": "Tres canales, misma base. 'directa' es la de descargas.dnns.es (con actualizacion propia) y es donde se prueba cada version. 'play' es la de Google Play e 'ios' la de iPhone (TestFlight/App Store), las dos sin actualizacion propia porque la tienda lo prohibe. Gradle se niega a empaquetar si la web puesta no es la del canal que se compila. El proyecto iOS (carpeta ios/) solo se compila en macOS: en Windows se hace el sync y el resto lo hace GitHub Actions.",
    "build:directa": "vite build --mode directa && cap sync android",
    "build:play": "vite build --mode play && cap sync android",
    "build:ios": "vite build --mode ios && cap sync ios",
    "apk:directa": "npm run build:directa && node herramientas/gradle.js assembleDirectaRelease",
    "aab:play": "npm run build:play && node herramientas/gradle.js bundlePlayRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.2",
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.6.9",
    "@kadena/cryptography-utils": "^0.4.4",
    "@kadena/hd-wallet": "^0.6.2",
    "blakejs": "^1.2.1",
    "buffer": "^6.0.3",
    "ethers": "^6.13.2",
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "vite": "^7.1.0"
  }
}
